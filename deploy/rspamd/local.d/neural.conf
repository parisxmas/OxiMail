# neural — TensorFlow/CNN-based spam classifier. Requires a redis
# backend to store the trained model and a long training pass to
# converge on a useful classifier. We run neither, so the module's
# model loading + per-message inference is pure overhead.
enabled = false;
