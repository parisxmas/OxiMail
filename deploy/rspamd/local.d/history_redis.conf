# history_redis — per-message history shipped to redis. We don't
# run redis; rspamd already logs every scan to its own log file
# (which we tail for diagnostics), and the web UI's history view
# isn't part of our operator workflow.
enabled = false;
