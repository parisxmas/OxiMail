# metric_exporter — periodic push of rspamd metrics to an external
# collector (graphite / influxdb). We don't push anywhere; rspamd's
# built-in /metrics endpoint on the controller is enough.
enabled = false;
