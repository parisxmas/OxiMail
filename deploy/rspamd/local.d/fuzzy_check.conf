# fuzzy_check — fuzzy (ssdeep / shingles) hashing against rspamd's
# public fuzzy servers. Useful at scale; for a personal mailbox the
# bandwidth + memory cost of the per-message hashing dance dominates.
# The corresponding fuzzy.zone maps don't load either.
enabled = false;
